[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  framework = "vite"
  targetPort = 3000

# Environment variables will be loaded from .env file
