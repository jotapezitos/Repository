# Copy to .env during development and fill values. DO NOT commit real secrets.
# Frontend (Vite) - safe to use public/anon keys only
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server (Netlify env vars) - sensitive values, set in Netlify UI
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
GOOGLE_API_KEY=
KIWIFY_CHECKOUT_URL=
KIWIFY_CLIENT_SECRET=
